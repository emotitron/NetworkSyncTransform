using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;
using UnityEngine;

namespace emotitron.Compression
{
	public static class TransformExtensions
	{

		// TODO: Expand these, or remove them
		//public static ReadOnlyCollection<ulong> CompressToArray64(this Transform t, TransformCrusher tc, BitCullingLevel bcl = BitCullingLevel.NoCulling)
		//{
		//	tc.Compress(CompressedMatrix.reusable, t);
		//	return CompressedMatrix.reusable.AsArray64(bcl);
		//}

		//public static ReadOnlyCollection<uint> CompressToArray32(this Transform t, TransformCrusher tc, BitCullingLevel bcl = BitCullingLevel.NoCulling)
		//{
		//	tc.Compress(CompressedMatrix.reusable, t);
		//	return CompressedMatrix.reusable.AsArray32(bcl);
		//}

		//public static ReadOnlyCollection<byte> CompressToArray8(this Transform t, TransformCrusher tc, BitCullingLevel bcl = BitCullingLevel.NoCulling)
		//{
		//	tc.Compress(CompressedMatrix.reusable, t);
		//	return CompressedMatrix.reusable.AsArray8(bcl);
		//}

	}
}

